name: Tests

on:
  pull_request:

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@charmcraft-tox-wrapper
    secrets: inherit
    with:
      self-hosted-runner: true
      self-hosted-runner-label: "edge"
